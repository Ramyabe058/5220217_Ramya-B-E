int diagonalDifference(int arr_rows, int arr_columns, int** arr) {

   int first_sum = 0;
   int second_sum = 0;
   
   for(int i = 0; i < arr_rows; i++){
    first_sum += arr[i][i];
    
    second_sum += arr[i][arr_columns - 1 - i];
   }
   
   return abs(first_sum - second_sum);
}